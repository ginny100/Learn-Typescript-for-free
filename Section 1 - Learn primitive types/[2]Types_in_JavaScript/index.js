//Types in JavaScript

function addNumbers(a, b) {
    return a + b
}

console.log(addNumbers("5", "6"))

/* We expect 11, but we actually get "56" as we don't specify types passed in this function */